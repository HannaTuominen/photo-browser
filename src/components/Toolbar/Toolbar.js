import React from 'react';

import './Toolbar.css';

const toolbar = props => (
  <header className="Toolbar">
    <div>
      <div>
        Logo
      </div>
    </div>
  </header>
);

export default toolbar;
